import React from 'react';
import Base from "../core/Base";

const Profile = ()=>{
 return (
        <Base title="profile">
         <h1>
             this is profile page
         </h1>
        </Base>
 );
};

export default Profile;